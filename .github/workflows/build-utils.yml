name: Utils

on: [push]

jobs:
  tests:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: build
      run: |
        mkdir build && cd build
        cmake .. 
        make inspect-kernel
