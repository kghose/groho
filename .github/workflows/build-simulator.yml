name: Simulator

on: [push]

jobs:
  Simulator:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: build
      run: |
        mkdir build && cd build
        cmake .. 
        make groho
